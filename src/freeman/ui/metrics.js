/* Renders the measurement and proportion panel into `root`.
   Placeholder: the analysis agent replaces this module. */
export function renderMetrics(root, metrics /* from measure() */, state /* trait values */) {
  root.replaceChildren();
}
